package sbnz.rules;

import model.NadmorskaVisina;

rule "proba"
    when
        $i: Item( cost <= 300, category == Category.NA )
    then
        $i.setCategory(Item.Category.LOW_RANGE);
        update($i);
end